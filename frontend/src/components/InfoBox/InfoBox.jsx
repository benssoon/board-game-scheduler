import './InfoBox.css';

function InfoBox({title}) {
    return (
        <>
            <h2>{type.charAt(0).toUpperCase() + type.slice(1)}</h2>
            <section>{type === "about" &&
            }</section>
        </>
    );
}

export default InfoBox;